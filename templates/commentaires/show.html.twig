{% extends 'dashboard/index.html.twig' %}

{% block main %}

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ commentaire.id }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ commentaire.message }}</td>
            </tr>
            <tr>
                <th>Date de publication</th>
                <td>{{ commentaire.datePublication ? commentaire.datePublication|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Voyage</th>
                <td>
                    {{ commentaire.voyageId.name }}
                    <a href="{{ path('voyage_details',{id:commentaire.voyageId.Id}) }}"><span class="badge badge-warning">consulter</span></a>
                </td>
            </tr>
            <tr>
                <th>Utilisateur</th>
                <td>
                    {{ commentaire.userId.email }}
                    <a href="{{ path('user_details',{id:commentaire.userId.Id}) }}"><span class="badge badge-warning">consulter</span></a>
                </td>
            </tr>
        </tbody>
    </table>


    <a class="btn btn-success" href="{{ path('commentaires_edit', {'id': commentaire.id}) }}">Editer</a>
    <br>
    <br>

    {{ include('commentaires/_delete_form.html.twig') }}
    <br>

    <a class="btn btn-primary" href="{{ path('commentaires') }}">Retourner à la liste des commentaires</a>
{% endblock %}
